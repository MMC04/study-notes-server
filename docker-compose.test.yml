# docker-compose.test.yml
services:
  test-db:
    image: postgres:16-alpine
    container_name: study-notes-test-db
    environment:
      POSTGRES_USER: testuser
      POSTGRES_PASSWORD: testpass
      POSTGRES_DB: study_notes_test
    ports:
      - "5433:5432"